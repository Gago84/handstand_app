rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Training videos are public content in the current product. Only trusted
    // administrators may upload, replace, or delete files through the console.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
